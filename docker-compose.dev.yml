version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: shankar_seeds_mongodb_dev
    restart: always
    command: ["--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo_dev_data:/data/db

volumes:
  mongo_dev_data:
